name: 'ecommerce_retail_analytics'
version: '1.0.0'

profile: 'ecommerce_retail_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  active_days_threshold: 180
  
  # Customer segmentation thresholds
  customer_high_value_threshold: 500
  customer_medium_value_threshold: 100
  customer_promoter_threshold: 4.5
  customer_neutral_threshold: 3.0

  # Seller segmentation thresholds
  seller_platinum_value_threshold: 10000
  seller_gold_value_threshold: 1000
  seller_silver_value_threshold: 100

models:
  ecommerce_retail_analytics:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
